# MongoDB connection URI
MONGODB_URI=mongodb://localhost:27017/react_task_manager

# Port for the Express server (optional, defaults to 5000)
PORT=3001

# Other environment variables (if needed)
# API_KEY=your_api_key
